name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
      - pages-auto
    paths:
      - docs/**
      - .github/workflows/pages.yml
      - .github/scripts/**
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Generate video thumbnails
        run: |
          shopt -s nullglob
          python .github/scripts/generate_dashboard_video_thumbs.py docs/dashboard/index.html docs/archive/*/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
